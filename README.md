# teacher_directory
This repo is created to take a test for PPMG. 
To run the project:

To do unit test:
    python3.8 manage.py test && flake8

To create database:
    for main:
    for test:
    ALTER ROLE ppmg CREATEDB;
